import {} from './projectqsActions.js/index.js';

const initialState = {};

const projectqsReducer = (state = initialState, action) => {
	switch (action.type) {
		default:
			return state;
	}
};

export default projectqsReducer;
