import {

} from "./homeActions";

const initialState = {
  welcome: "Hello, this is placeholder text :)"
};

const homeReducer = (state = initialState, action) => {
  switch (action.type) {

    default:
      return state;
  }
};

export default homeReducer;