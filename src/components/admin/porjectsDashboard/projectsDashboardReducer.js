import {} from './projectsDashboardActions';

const initialState = {};

const projectsDashboardReducer = (state = initialState, action) => {
	switch (action.type) {
		default:
			return state;
	}
};

export default projectsDashboardReducer;
